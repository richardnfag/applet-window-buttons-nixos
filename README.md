## NixOS Package for Window Buttons Applet

This is a NixOS Package for [Window Buttons Applet](https://github.com/psifidotos/applet-window-buttons)

### Install

```sh
nix-env -f  applet-window-buttons -i $(nix-build)
```
